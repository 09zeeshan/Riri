{
  "name": "ai-studio-applet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "serve public -l 3000 --no-port-switching",
    "build": "echo 'Static site, no build needed.'",
    "start": "serve public -l 3000 --no-port-switching"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
